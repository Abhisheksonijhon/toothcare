
## v2 changes
- Fixed: mobile menu was clipped to the header height (menu is now outside the header), oval symptom chips, weak doctor placeholder.
- New: dark animated hero with sparkles and glow around the 3D tooth, rotating age words, yellow ticker, "Dental care for every age" slider (kids, teens, adults, seniors), colour-coded treatment cards, scroll reveals.
- New: 3 more articles (braces for adults, sensitive teeth, care after 50), 3 more illustrations, blog filters by age group and topic (`blog.html?for=kids` works).
- SEO URLs are currently set to https://abhisheksonijhon.github.io/toothcare for testing. Rebuild with your real domain before launch.

## Updating GitHub after a new version
Unzip, then copy everything inside `site/` over your repo folder (replace files). Then:
```
git add .
git commit -m "Design update v2"
git push
```
Hard refresh the live page (or open it in a private tab) if you still see the old design.
